{
  "name": "olympus-sng",
  "private": true,
  "description": "올림포스 도시 — 정적 클라이언트 + Firebase 백엔드(functions/). 개발용 툴체인만 여기 둔다(클라이언트는 빌드 도구 없이 그대로 GitHub Pages에 서빙).",
  "scripts": {
    "sync-heroes": "node scripts/sync-heroes.js",
    "emulators": "npm run sync-heroes && firebase emulators:start",
    "gen-assets": "node tools/gen-assets.mjs"
  },
  "devDependencies": {
    "firebase": "^12.17.1",
    "firebase-admin": "^14.2.0",
    "firebase-tools": "^15.26.0"
  }
}
