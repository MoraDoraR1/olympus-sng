{
  "name": "olympus-sng-functions",
  "private": true,
  "description": "올림포스 도시 백엔드 — Firebase Cloud Functions (Node/Express/SQLite server-legacy 대체)",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "pretest": "node ../scripts/sync-heroes.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2"
  }
}
